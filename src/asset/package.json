{
  "name": "asset",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && ./node_modules/.bin/bower install",
    "jadec": "./node_modules/.bin/jade jade --hierarchy --out html",
    "jade": "./node_modules/.bin/jade -w jade --hierarchy --out html",
    "dev": "echo using 'node $(node -v)'; npm install && npm run jade & gulp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "connect-livereload": "^0.5.3",
    "express": "^4.13.3",
    "gulp": "^3.9.0",
    "gulp-livereload": "*",
    "jade": "*",
    "bower": "*"
  }
}
